# Implementation overview

**Prepared implementation work:** [concepts](concepts.md) defines Storyteller turns, decision points, scenes and passages, and excludes chapters. [Bounded-cost B1](features/2026-09-19--19-08--bounded-storyteller-cost/PLAN.md) implements pure effective-policy resolution, fail-closed provider admission, immutable one-shot task recipes/resource envelopes and whole-request byte preflight. The admitted authority is retained by the task/story and all later task kinds. Synthetic tier fixtures are isolated behind developer tools. Next is B2 operation/window debits and holds. [Canonical storage and creative bundles](features/2026-09-19--18-49--canonical-campaign-storage/PLAN.md) C1/C2 remains independently ready; [memory and exploration](features/2026-09-18--20-09--storyteller-memory-and-recall/PLAN.md) consumes storage and the budget controls. No live evaluation has begun. Existing creative profiles are JSON files; campaign prose/notes still reside in PostgreSQL.

**Current focus:** implementation is active. Bounded S2/U2a supports quiet factual completion, exact A→B→A handoff, independently repeated authored routines, durable story-scoped completion limits and boundary-blocked prerequisites. U2b reports preserve historical evidence without replacing current authority. S3 lets the Storyteller select an exact retained activity. U2c now supports two-to-six-entry linear accepted plans, pending-only cancellation, explicit successor horizons, visible prerequisite blocking and explicit re-entry after a controlling scene: publication re-offers the exact queued commitment but only the player's selection restarts it. The [gold session](technical/playthroughs/harbor-session.md) remains the connected acceptance target; it is not yet a recorded play session.

The cost feature's exact next slice is B2 durable operation allocations, fixed/rolling windows and game hold/recovery. [Usage policy](technical/usage-policy.md) separates renewable quotas from nonrenewing funding and defaults to explicit Resume. Policy resolution and task admission work offline, but commercial account selection, window consumption and player-visible holds do not. Before any explicitly authorized first live smoke test, its selected safety path must work offline; the prepared initial ceiling is one request, 8k input / 1,024 generated tokens and $0.01, not standing permission or a commercial tier promise.

**Observability:** a player-safe append-only activity ledger records starts, suspension-by-switch, exact resumes, manual pause/resume, blockers, interruptions, completion-pending and completion atomically with mechanics. The play surface renders the newest 100 events separately from current commitments, so completed work remains readable after reload. API/worker lifecycle incidents and retryable outbox failures use safe structured JSON events with correlation. The developer-only Chamber inspector exposes activity-report hook/source identity and generation/publication status plus the accepted plan's revision, cursor, horizon, entry bindings, stop reason and current authorization without private plan payloads. Deterministic failure controls and a manual QA run remain missing.

[Multiple suspended commitments](features/2026-09-19--02-24--multiple-suspended-commitments/FEATURE.md) partially implements retention through A → B → A, but its test completes B through direct database writes. It does not prove real B settlement, monotonic chronology or browser experience. [Storyteller agency and taste](features/2026-09-19--00-26--storyteller-agency-and-taste/FEATURE.md) has structural evidence with owner taste outstanding; the atlas identifies unchanged Gary-failure menus and an instant beacon option whose prose promises waiting. [Inhabitable return](features/2026-09-19--00-26--inhabitable-play-and-return/FEATURE.md) and [premise-driven setup](features/2026-09-19--00-26--premise-to-playable-campaign/FEATURE.md) remain roadmap work.

**Lifecycle:** pre-POC, solo development on `main`. Live inference remains disabled. Provider adapters and accounting exist, but no live route, balance or model quality is verified. A stored key and the original $10 deposit are not authorization to spend.

**Persistence/cache boundary:** PostgreSQL remains the sole authority and Drizzle access stays in application-domain operations rather than controllers or workflows. Story snapshots now support an optional owner-checked, projection-versioned Redis cache through an application port and separate infrastructure adapter. Redis failure falls back to the existing repeatable-read projection; commands, timers, private plans and accounting never use cached authority. Lists/history remain uncached pending workload evidence.

**Experience direction:** activities and chains separate mechanical execution, reporting and continuation. Activity A1/A2 own clock, identity and contribution/wait rules; [authored situations](features/2026-09-19--13-49--storyteller-authored-situations/PLAN.md) own choices/current activity permission; [bounded autonomy](features/2026-09-19--00-26--bounded-autonomy-and-reentry/PLAN.md) owns quiet settlement, historical reports, controlling scenes and finite chains in separate slices. First prove independently selected quiet work and consecutive scene-only turns, then accepted continuation. Current completion always requests consequence narration; the correction makes it selectable, not forbidden. Broad cooperation and unattended decision fallbacks remain later work, not prerequisites for this first offline proof.

The Storyteller explicitly authors each mechanical situation's immediate choices and exact process/resume access; inventory/capabilities never generate extra options. Situation authorization privately retains unconsumed or explicitly repeatable activity plans while work runs. Quiet completion mechanically revalidates and projects only those plans under a fresh offer; each repeat creates a fresh instance and receipts. Limited definitions persist completed counts outside transient offers, disappear at their story-scoped limit and cannot be duplicated while an earlier instance remains open. Boundary-rechecked prerequisites block work before another attempt or reward, retain its progress and require a later explicitly authorized resume. A controlling scene replaces current authority without resetting occurrence history. Actor/site scope and irrecoverable target invalidation wait for concrete identity models.

## Implemented boundary

| Area | Exists now | Important limit |
| --- | --- | --- |
| Workspace | Next.js web, NestJS API, Temporal worker/workflows, PostgreSQL; shared game, contracts, application and Storyteller packages | See the code map for actual wiring; architecture specifications also describe unbuilt capabilities |
| Identity and creation | Stored sessions, GitHub OAuth integration, owned versioned drafts, profile selection, persistent opening review and idempotent Start | Real OAuth needs credentials/manual verification; local Chamber supplies a development session |
| Narrative rehearsal | Authored opening, choices, continuity notes, saved waits, pause/resume, story list and history | Exercises persistence and UI, not arbitrary-premise generation |
| Mechanical actions | Generated public offers backed by immutable private immediate-action plans and matching explicit activity authorization; unconsumed activity plans can survive quiet selections and be freshly reprojected; repeatable instances, story-scoped completion limits and boundary-blocked prerequisites are durable; direct exactly-once immediate receipts and recoverable scene narration | Actor/site occurrence scope and irrecoverable target invalidation await concrete identity models. The behavior is fixture evidence, not general Storyteller intelligence; human taste remains unverified |
| Mechanical policy | Pure validation of shape, evidence, character/story facts, quantity minimums, abilities and skills; explicit story-fact declaration with receipt provenance; modifiers derived from character state | No general rule/target invention or story-fact update/retirement; ungrounded situational modifiers rejected |
| Character capabilities | Selected D&D scores plus currently applicable abilities and declared skills/proficiencies | Mechanical creation still begins from developer-authored character seeds; no admitted capability-changing transformation |
| Content | Validated JSON mechanical seeds, narrative rehearsal and creative profiles; server-supplied mechanical catalogue summaries | No species/world-specific engine branches; fixtures do not establish general world understanding |
| Time and settings | Narrative prepared intervals; campaign-owned exact mechanical clock; typed contribution and clock-wait rules; event interruption, exact-instance progress-preserving resumption and completion-pending state; editable/lockable creative and speed settings | Persisted real B settlement/browser experience remains unverified; quiet completion, explicit abandonment, named-capacity concurrency and broader process families are unimplemented |
| Storyteller tasks | Opening, continuation, consequence and strict report-only contracts; mechanical opening and consequence return freshly validated private plans; bounded context, immutable requests, execution, publication and recovery | Report admission/storage/historical projection are not implemented. Offline source contains explicit benchmark branches and holds unknown mechanical seeds; no pre-narrative retrieval rounds or live-model quality evidence yet |
| Context | Recent passages plus mandatory source-backed notes; consistency and overflow rejection | Every note source is mandatory raw input; no archived-memory lookup or scene/entity retrieval. The 20-note/48-KiB limits do not establish long-story recall |
| Provider/accounting | Explicit opt-in adapter; resolved authority and route matched at admission; immutable narrowed task envelope retained by the story; persistent attempt audit linking effective policy, funding/run/owner/story-or-draft/task/profile and configuration versions; labelled estimate versus conservative reservation; dispatch/settlement timing and latency; normalized provider token/cache/reasoning usage; reported/calculable cost reconciliation; sanitized per-story totals/recent attempts in Chamber; uncertainty stop and injected fake transport | Durable operation/window allocations and holds are not yet enforced. Cache-price calculation intentionally remains unavailable until route snapshots model it. Offline only; no verified real spending, commercial selection or operator provisioning UI |
| Exploration | Local Chamber inspection, manual QA catalogue, durable run/evidence records and sanitized export | Rich trace explorer and planned fault/mechanical scenarios remain unfinished |
| World/showcase | Small authored transfer and chronology examples | General inventory/combat/travel, shared worlds, notifications and public visitor onboarding remain unimplemented |

## Where to resume feature work

The [playable DM adjudication loop plan](features/2026-09-18--17-21--playable-dm-adjudication-loop/PLAN.md) remains paused as a standalone infrastructure track. Its authority work is now consumed by the active [agency/taste plan](features/2026-09-19--00-26--storyteller-agency-and-taste/PLAN.md): offer-local private plans, direct resolution, scoped fact admission and bounded opening/consequence planning support state-shaped offline turns. Continue through the experience roadmap rather than completing mechanical subsystems in isolation.

Immediate selection resolves directly without an activity row or tick advance. Its receipt, optional roll, effects, story-fact declarations, consumed offer and follow-up commit before a separate worker operation prepares a DM turn. Mechanical opening review now projects freshly proposed plans and Start stores those exact private plans. Consequence publication independently revalidates its plans and stores their public projection atomically with the new passage. This is an execution kernel, not yet evidence of an enjoyable or time-dependent game.

The same opaque public attempt can reference a private contribution-process or resume plan. Starting records a zero-progress commitment; an event can suspend it in `encounter`; immediate responses leave that activity intact. Starting different admitted work changes the interrupted or paused row to `suspended` instead of abandoning it. The campaign pointer names only the one activity allowed to advance; all unfinished rows remain projected as compact commitments, stale wake-ups fail the pointer fence, and a unique eligible resume plan reactivates the same activity ID, clock cursor and earned contribution. Explicit abandonment, durable capacity claims and blocked/invalidated resume outcomes remain unfinished lifecycle work.

For continuing-life play, implement bounded-cost B1/B2 and storage C1/C2, then the [prepared memory phases](features/2026-09-18--20-09--storyteller-memory-and-recall/PLAN.md): provenance/context capture, linked source-bounded summaries, bounded exploration, return rehearsal and local semantic/hybrid search. Creative bundle storage has its own C3 phase. The first product acceptance is a fresh Storyteller turn using a meaningful old detail correctly in a playable return, with current state, original evidence and bounded total work intact. No chapter engine, full scene lifecycle or runtime subagent framework is a dependency.

The [offline acceptance contract](engineering/offline-poc-acceptance.md) defines the manual flow and responsibility-specific probes. The [trace explorer](features/2026-09-18--17-27--storyteller-trace-explorer/FEATURE.md) remains planned. Contribution-based activities, spatial movement and richer rule domains are separate designs; do not turn an example's duration, anatomy or currency into a universal mechanic.

Navigation follow-up should examine story timing/recovery and database record relationships next. Normal HTTP story composition currently passes through the Chamber wrapper; document its actual authorization and delegation before any later separation. No restructuring is implemented by this note.

## What can be tried

Launch `pnpm chamber` and use `/stories` to select a storyteller, save a draft, review an opening and Start. Narrative rehearsal can exercise saved choices, continuity, waits and return visits. Select mechanical content in opening review to inspect generated offline plans and dice consequences. `/demo` is a separate browser-only presentation prototype with manual time and reset-on-refresh state.

Use [development](development.md) for setup and [QA journeys](engineering/qa-journeys.md) for manual evidence recording. An authored rehearsal is not proof of a functioning generative DM.

## Verification evidence and limits

For the current A1/S1/A2/S2-U2a checkpoint, affected packages and production web build pass, with 24/24 game and 30/30 Storyteller tests passing. The cost-accounting slice rebuilt Storyteller/database/application/integration packages, regenerated and applied the single disposable baseline, and passed both fake-provider accounting cases including rich row assertions. The broader Storyteller integration currently has unrelated activity-fixture expectation failures and a browser timeout; these are not represented as accounting failures or hidden as a green suite. Repeatable independent routine selection, owner taste review and a manual wall-clock rehearsal remain outstanding.

Checks remain optional under [verification policy](../.agents/rules/verification.md). The preceding readiness pass checked document links/anchors; A1 added the focused build/test evidence above without a persisted runtime session. Historical build failures that were subsequently fixed are not current blockers. Passing compilation or scripted tests does not certify meaningful agency, live continuity, prose quality or general mechanics.

The persistence/cache slice compiles across application, cache, API and integration workspaces. Its focused disposable-database case verifies a snapshot hit, projection-version miss, malformed-value fallback and owner rejection; a real local Redis adapter probe verifies bounded serialization and expiry plumbing. These checks establish cache consistency boundaries, not production hit rate or latency benefit.

Live evaluation requires the owner to reopen it explicitly under [spending rules](../.agents/rules/spending.md), after offline flow/trace work and a concrete bounded evaluation design. No provider calls occurred in this pass: $0 provider spend; cumulative account usage unverified.
